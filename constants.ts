import { MessageSender } from './types';
import type { ChatMessage } from './types';

export const TERMINATION_PHRASE = "Houston, we have a problem.";
export const QUIZ_INTERVAL = 5;
export const TOTAL_FACTS_PER_LEVEL = 15;